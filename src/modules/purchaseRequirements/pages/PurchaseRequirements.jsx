import PurchaseRequirementsPanel from '../components/PurchaseRequirementsPanel';

// Standalone Purchase Requirements page (Mill / inventory context). The Finance
// view is embedded on the Finance Dashboard instead — see FinanceOverview.
export default function PurchaseRequirements() {
  return <PurchaseRequirementsPanel />;
}
